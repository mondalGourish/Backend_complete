what is server?
it is a location where we send request and ger response
for backend we have to create a server, for that we have to write some code, and for that we need a language
In js we can use Node js to run the code in the server side
Now we don't have to write the server code from scratch, we have packages in npm which we can install(after installation the usage of the code is provided in the documentation)
After installing we get 2 files and 1 folder, node modules, package-lock.json and package.json

node modules -> store the code of the package
package.json -> it maintains the packages on which our code depends

------MAIN backend------
1. How to start a backend application?
->When we are using js and creating a server we can call it a NODE js application, and to start a NODE js application the best way is to run the command "npm init -y" int the terminal

2.How to create a server?
->With the help of express package we can create a server "npm i express"

3.Then we have to create a file called server.js and have to setup and run the server
const express = require("express")
const app = express()
app.listen(3000)

for receiving and sending data we use req,res
app.get("/",(req,res)=>{})
req-> whatever data is coming from frontend the only way to get is req
res-> to send data back


//API 
The set of rules and protocols we use to communicate between two different application is API(ex: frontend and backend)

A REST API (Representational State Transfer API) enables communication between client and server over HTTP. It exchanges data typically in JSON format using standard web protocols.

Uses HTTP methods like GET, POST, PUT, PATCH, and DELETE.
Client sends requests to server endpoints (URLs).
Server returns responses such as JSON, XML, HTML, or images.
Maps HTTP methods to CRUD operations (Create, Read, Update, Delete).

GET -> for fetching data from server
POST ->for sending data from fronted to server
PATCH ->to update data in the server/backend
DELETE ->to delete the data from server

//Production level coding
we need to create a src folder -> inside that app.js to create our server and inside the root folder we create server.js which helps to start the server

//In backend we don't have data from frontend so we have to use some intermediate thing, there comes postman into picture.
We can send data into backend from postman

.To always run the server use npx nodemon server.js

//Upto this we have leaned about postman and using backend apis get,post,patch delete using postman but each times when server reloads the data got vanished, not Database comes into picture where we cans store data and perform the functionalities

In mongoDb we can create cluster, it is a basically a machine with ram,cpu and storage
inside the cluster we can create out database

First of all we have conner our server with our database, for that we give access to all through the ip 0.0.0.0/0, when in development phase in production phase we only give access through the machine

Lets go through the database setup
open mongodb -> create a project -> create a cluster -> change the network and database access if required -> go to mongoDBcompass -> paste the copied connection string in the connections and you will see it connected

now after the setup is done we have to connect our server with the database for that use use the package "mongoose"

the logic of the the database connection is written under db.js
src -> folder(db) ->db.js(file)

Now before storing any data to the database our database should not how the structure of the data should look like, that where we need to design schema

now to create schema we create a folder under src named modules
src -> models(folder) ->name.model.js(file)


then we can write our apis in app.js for the functionalities
